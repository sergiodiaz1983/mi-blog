#!/usr/bin/env node

var Agent = require('../lib/agent');

var alice = new Agent({
    name: "Alice",
    port: 1338
});

alice.on('start', function () {
    console.log("Alice started");
});

process.on('SIGINT', function() {
    alice.stop();
    process.exit();
});


alice.start();

